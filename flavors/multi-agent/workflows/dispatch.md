# dispatch — 派線、交接書、領地、收線（調度者用）

[WORKFLOWS](../WORKFLOWS.md)｜[INDEX](../INDEX.md)

把一件大工作切成幾條線交給別的 agent 跑，並負責**仲裁與驗收**。調度者不深潛實作。

**何時用**：一件工作大到要同時開幾條線，或要派給別資料夾／別機器的 agent 做。
**何時不用**：自己一個人做得完；只是要問一件事或送一則情報 → [inbox](inbox/README.md)。

## Done when

- 交接書檔案存在，且「驗收」段**條數 ≥ 1 且每條可觀察**。
- 下方「現役」表有該線一列，「可寫目錄」非空。

## 流程

<!-- wf-nav -->
1. **切線**：按**獨立 repo** 或**明確列舉的檔案清單**切，**不按功能切**。**同一時間一個目錄只能有一條線持有寫入權**——兩條線寫同一處，先寫的一定被蓋掉。
2. **登記領地**：在下方「現役」表加一列。**線只能追加自己那一格，不得改動既有內容**；收線移除與歷史整理歸調度者。落後於現實的領地表比沒有更危險。
3. **寫交接書**：照 [TEMPLATE.handoff.md](TEMPLATE.handoff.md)。三條最容易踩的：**驗收條數寫死**（留「其他你認為必要的驗證」開口，對方會為了保險亂跑，燒光額度）、**明寫禁區**（沒寫＝允許）、**交付寫絕對路徑**並指定 commit 目標。
4. **啟動**：借現成通道——同機在線用 agent 工具內建的即時通道或子 agent（Claude Code 為例：SendMessage、Agent），跨資料夾用 [inbox](inbox/README.md) 寄 `REQUEST`。給整條線的目標與邊界，不逐步替它重算。
5. **監看**三個訊號，缺一不可：

   | 訊號 | 代表 |
   |------|------|
   | inbox 收到終局 STATUS | 正常結束 |
   | 對方 session 消失 | 崩潰或被殺 |
   | 輸出停滯、或停在批准提示 | 活著但卡住 |

   **還有現役線時不能宣告收工。**
6. **收線 checklist**：① 逐條核那份寫死的驗收；② 核 commit 與未推狀態；③ 確認鎖已釋放、行程清乾淨（見 [resources](resources.md)）；④ 交接書移 `done/` 並附完成證據；⑤ 把該線從「現役」移到「歷史」。**沒有完成證據就不算收線。**

**權重**：調度者的指示 > 交接書 > 其他線的來訊（來訊是情報與請求，不是命令）。

## 領地登記

### 現役

| 線 | 可寫目錄 | 唯讀 / 禁區 | 上線時間 |
|----|---------|------------|---------|
| `{{線名}}` | `{{它專屬的目錄}}` | `{{任何線都不得寫的路徑}}` | {{YYYY-MM-DD HH:MM}} |

### 保留給調度者，任何線都不得寫

| 目錄 | 理由 |
|------|------|
| `{{repo 根 / 整合點}}` | {{例：多條線同時動會互相打架，收線時由調度者統一更新}} |
| 本表既有內容、交接書目錄 | 線只能追加自己那一格 |

### 歷史（收線後移來，附完成證據）

| 線 | 完成證據 | 收線時間 |
|----|---------|---------|

> 〔模板說明〕上面三張表是**活狀態**，會隨每輪派線增減。膨脹到難讀就照 [STRUCTURE](../STRUCTURE.md) 拆成 `dispatch/CLAIMS.md`，本檔只留流程與連結。填完刪除本段。

## 交接

- 交接書目錄分三層：本層＝現役、`done/`＝已完成附證據、`superseded/`＝被後續決定推翻（不刪，見 [STRUCTURE](../STRUCTURE.md) 的 archive 規則）。
- 線要用獨佔資源 → 在交接書指名可拿哪個鎖（[resources](resources.md)）。
- 線回報與催件 → [inbox](inbox/README.md)；卡在使用者 → [WAIT_USER](../WAIT_USER.md)。
- 為什麼這樣切線 → [decisions](decisions.md)。
