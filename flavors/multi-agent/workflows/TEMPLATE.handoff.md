# 任務：<動詞開頭的一句話>

> 複製本檔改寫成一份交接書，放進交接書目錄（現役在頂層、做完移 `done/` 附證據）。派線規則見 [dispatch](dispatch/README.md)。
> **交接書是唯一契約**：沒寫在裡面的授權等於沒有；沒寫的禁區等於允許。

## 背景與唯一目標

<為什麼要做這件事，以及**一個**目標。兩個以上的目標＝該切成兩條線。>

## 工作

1. <步驟>
2. <步驟>

## 硬性限制

- **禁區**：`<這條線不得寫的路徑>`（唯讀或完全不碰）。
- **可拿的鎖**：`<資源鎖清單；一個都不給就寫「不取任何鎖」>`（規則見 [resources](resources.md)）。
- **不 push**；commit 到 `<repo>` 的 `<分支>`，push 等使用者確認（守鐵律 2：授權來源）。
- **暫存與丟棄物放哪**：`<大檔丟 /tmp/<線名>-trash/、KB 級文字丟 <交接書目錄>/<線名>/trash/>`；**不要 `rm`**。
- **整檔改寫用單一 Update 操作**，不要拆成 Delete＋Add（理由見 [dispatch/lessons](dispatch/lessons.md)）。
- <其他：不得動的設定檔、不得啟動的程式、額度上限…>

## 交付

| 產物 | 路徑（絕對路徑）|
|------|----------------|
| <是什麼> | `<絕對路徑>` |

## 驗收（條數寫死，不要自行加碼）

1. <可觀察條件：檔案存在／指令回傳／表格填滿>
2. <…>

**就這 N 條，不要做「其他你認為必要的驗證」**——加碼驗證會燒光額度且無人要求。

## 完成／阻塞回報（必做）

- 我是誰：`<線名>`。本線團隊：`teams/<TEAM>`（沒有就寫「無」）。
- 回報方式（擇一，照專案的 inbox 佈局）：
  - **基本佈局**（單一收件匣）：`tools/inbox_send.sh <上游的 inbox 路徑> <線名> <STATUS> '<一句自足結論>' [正文檔]`
  - **升級後佈局**（五通道）：`tools/inbox_mail.sh <線名> --up <STATUS> '<一句自足結論>' [正文檔]`
    ——它會依團隊名冊自動送到你的領導，**你不需要知道上游叫什麼名字**。
- 結束時必發終局 STATUS 之一：`DONE` / `BLOCKED` / `NEEDS-USER` / `FAILED`。
- **卡住超過 10 分鐘也要先發 `BLOCKED`**（需使用者決定用 `NEEDS-USER`），不可無聲等待。
- 標題要是自足的一句結論，不可只寫「完成」「報告」。正文照四段格式。
- 完整契約：[inbox/PROTOCOL.md](inbox/PROTOCOL.md)。

> 送出前對照一次「背景與唯一目標」和「工作」有沒有互相矛盾——衝突處要寫死哪個為準。
