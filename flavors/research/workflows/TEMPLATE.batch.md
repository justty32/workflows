# TEMPLATE.batch — 候選批次骨架

[backlog](backlog.md)｜[WORKFLOWS](../WORKFLOWS.md)

> 複製下面整塊改寫成 `backlog/<批次名>.md`，並在 `backlog/README.md` 的「現役」表補一列。frontmatter 四欄不要省——整批凍結之後，就靠它回答「當初為什麼選這批」。

````md
---
purpose: <為誰、為哪個問題掃的；例：為 <某專案> 的 <某章節> 找可用文獻>
workflow: workflows/source-intake.md
status_legend: "狀態：⬜ 待處理 / 🔶 部分完成 / ✅ 完成"
note: <證據強度與範圍限制；例：僅摘要層快篩、未通讀全文，數字均為作者自報>
---

# <批次名> — <一句話：主題方向 / 時間窗>

<兩三句：這批怎麼掃出來的（來源、時間窗、去重方式），以及**留下的標準**——只留會實際改變既有判斷的，不因為新就收。>

## 候選

| id | 題名 | 主題 | ★ | 狀態 | 接點註記 |
|----|------|------|---|------|---------|
| <材料 id> | <題名> | <主題方向> | ★ | ⬜ | <打到自家哪一節、有何啟發；沒有就寫「—」> |
````

## 欄位說明

| 欄 | 放什麼 |
|----|--------|
| `id` | 材料 id（DOI / URL slug / 報告編號）；沒有 id 的材料另開一批，別混進來 |
| `★` | 優先處理；不優先就留空 |
| `狀態` | 隨 [source-intake](source-intake.md) 逐件更新，與 `index/` 同一套圖例 |
| `接點註記` | 當初為什麼選它。這欄是凍結後唯一還有用的東西，別留空 |

⬜ 歸零後整檔移進 `backlog/archive/`，之後不再改狀態（見 [backlog](backlog.md)）。
