# decisions — 決策記錄（為什麼選 A 不選 B）

[WORKFLOWS](../WORKFLOWS.md)｜[INDEX](../INDEX.md)

留下「為什麼」：git log 找得回改了什麼，找不回**為什麼放棄方案 C**。活狀態記「未完的」，這裡記「已定但要留理由的」。

**何時用**：在兩個以上可行方案裡選了一個，且日後可能被問「當初為什麼」。
**何時不用**：只有一條路，或改了隨時可回頭的小事。決策過程本身要結構化評估 → knowledge 包的 decide 工作流，結論再落到這裡。

## Done when

- 下表新增一列，「未選方案與原因」與「前提」兩欄非空。

## 記錄（新的在上）

| 日期 | 決定 | 未選方案與原因 | 前提（改變就要重看）|
|------|------|---------------|-------------------|
| 2026-08-14 | 資料存單一 JSON 檔 `~/.todo.json`，整份讀進來、整份寫回去 | SQLite：要帶 native 相依、安裝變複雜，單人小資料量換不到效能；每筆一檔：目錄會散、`list` 要開幾百個檔 | 資料 < 1 萬筆、同時只有一個 process 在寫。任一條不成立就要重看（先考慮寫入用 tmp 檔＋rename） |
